@model IEnumerable<WebApplication2.Models.Titles>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TitleName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Federation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TitleName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Federation.FederationName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TitleId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TitleId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TitleId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
